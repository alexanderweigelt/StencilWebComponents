{
  "name": "root",
  "description": "Building Web Components with Stencil.js",
  "private": true,
  "scripts": {
    "dev": "BUILD_ENV=development lerna run dev --parallel",
    "build": "BUILD_ENV=production lerna run build --stream",
    "start": "lerna run start",
    "test": "lerna run test"
  },
  "workspaces": [
    "app",
    "packages/*"
  ],
  "dependencies": {},
  "devDependencies": {
    "lerna": "^8.0.2"
  },
  "license": "MIT"
}
